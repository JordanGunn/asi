{
  "step": 4,
  "output": {
    "validations": [
      {"mechanism": "validate.sh --check prereqs", "validates": "Plan directory exists, no conflicting active plan", "failure_behavior": "Exit 1 with error message"},
      {"mechanism": "validate.sh --check schema", "validates": "active.yaml conforms to plan_v1.schema.json", "failure_behavior": "Exit 1 with schema violation details"},
      {"mechanism": "validate.sh --check steps", "validates": "All steps have valid IDs, statuses, no duplicates", "failure_behavior": "Exit 1 with step errors"},
      {"mechanism": "validate.sh --check all", "validates": "All above checks", "failure_behavior": "Exit 1 on first failure"}
    ]
  }
}
