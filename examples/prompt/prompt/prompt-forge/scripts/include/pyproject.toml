[project]
name = "prompt-forge-cli"
version = "0.1.0"
description = "Deterministic CLI for prompt-forge skill in nunchuck"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "jsonschema>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
