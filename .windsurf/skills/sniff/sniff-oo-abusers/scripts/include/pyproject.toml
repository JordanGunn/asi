[project]
name = "sniff-oo-abusers"
version = "0.1.0"
description = "CLI for sniff-oo-abusers"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
