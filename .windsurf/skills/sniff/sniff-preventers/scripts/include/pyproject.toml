[project]
name = "sniff-preventers"
version = "0.1.0"
description = "CLI for sniff-preventers"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
