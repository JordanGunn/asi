# Never

- Never infer steps not explicitly requested by user
- Never auto-complete steps without verification
- Never reorder steps without explicit consent
- Never delete steps — only skip with consent
- Never modify step descriptions after creation without consent
- Never execute tasks — only track them
- Never edit plan files directly — use scripts only
- Never archive with incomplete steps without --force and user consent
- Never create multiple active plans
