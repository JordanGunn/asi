{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Skill Type Decision",
  "description": "Agent decision: single skill or grouped skills. Produces SKILL_TYPE.json artifact.",
  "type": "object",
  "required": ["type", "rationale", "indicators"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["single", "grouped"],
      "description": "Whether the skill is a single skill or a group of related skills"
    },
    "rationale": {
      "type": "string",
      "minLength": 1,
      "description": "Why this classification was chosen"
    },
    "indicators": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "Observable signals from user request that led to this decision"
    }
  },
  "additionalProperties": false
}
