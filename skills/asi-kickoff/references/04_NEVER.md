# Never

- Never implement scripts or code during kickoff
- Never populate reference documents with procedural content
- Never invent non-canonical reference files
- Never infer requirements not explicitly stated
- Never expand scope beyond declared skill purpose
- Never assume persistence or runtime environment
- Never proceed if skill name is ambiguous
- Never proceed if skill purpose is ambiguous
- Never skip the deterministic surface mapping step
- Never hide judgment decisions as deterministic
- Never answer open questions — capture them only
- Never speculate about missing constraints
- Never produce KICKOFF.md without all required sections
- Never overwrite existing KICKOFF.md without explicit consent
